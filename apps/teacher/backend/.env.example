# Mindforge Teacher Backend — PostgreSQL
# Copy to .env

# Database (PostgreSQL only)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=mindforge_teacher

# Server
TEACHER_SERVICE_PORT=3003
NODE_ENV=development
