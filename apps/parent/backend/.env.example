# Mindforge Parent Backend — PostgreSQL
# Copy to .env

# Database (PostgreSQL only)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=mindforge_parent

# Server
PORT=3002
NODE_ENV=development
